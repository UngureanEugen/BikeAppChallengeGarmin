package com.bike.app.data

class Converters {
    /*    @RequiresApi(Build.VERSION_CODES.O)
        @TypeConverter
        fun fromColor(color: Color): Int = color.toArgb()

        @TypeConverter
        fun toColor(value: Int): Color = Color(value)*/
    /*

        @TypeConverter
        fun toLong(color: Color): Long = color.value.toLong()

        @TypeConverter
        fun toColor(value: Long): Color = Color(value)
    */

    /*    @TypeConverter
        fun fromBikeType(value: String): BikeType {
            return BikeType.from(value)
        }

        @TypeConverter
        fun bikeTypeToString(bikeType: BikeType): String {
            return bikeType.type
        }

        @TypeConverter
        fun toWheelSize(value: Int) = enumValues<WheelSize>()[value]

        @TypeConverter
        fun fromWheelSize(value: WheelSize) = value.ordinal*/
    /*
          @TypeConverter
          fun fromWheelSize(value: String): WheelSize {
              return WheelSize.from(value)
          }

          @TypeConverter
          fun wheelSizeToString(bikeType: BikeType): String {
              return bikeType.type
          }*/

}